# .NET build output
api/bin/
api/obj/
api/client-dist/
publish/

# Node (client)
client/node_modules/
client/dist/

# Uploaded product photos
api/uploads/

# Stray Plesk placeholder that sometimes syncs into this folder locally —
# not part of the app, never intentionally committed.
/index.html

# Logs
*.log
npm-debug.log*

# Editor / OS
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json

# Local scratch space for throwaway scripts
.scratch*/
.e2e/
.localcheck/
.ftpcleanup/

# Node backend (restored) — local db, env, build output
server/.env
server/prisma/*.db
server/prisma/*.db-journal
server/dist/
server/node_modules/

# Deploy staging/packaging artifacts
deploy-staging/
deploy-delta/
*.tar.gz
*.zip
publish-smoketest*.log
cpcookies.txt
